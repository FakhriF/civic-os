{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Bun's ambient runtime types (the only API-specific need; every other
    // compiler option comes from the shared base config).
    "types": ["bun-types"]
  }
}
